import { styled } from "styled-components";

const Wrapper = styled.div`
   min-height: 1024px;
   overflow-x: hidden;
`;

const WorkWindow = () => {
   return (
      <Wrapper>
      </Wrapper>
   )
}

export {WorkWindow};